# Cards Against Reality app env template
# Copy to .env.local for local testing (do not commit real secrets)

# Public/base URL for redirects after Stripe checkout
APP_URL=http://localhost:3000

# Stripe test secret key (server-side only)
STRIPE_SECRET_KEY=sk_test_replace_me
