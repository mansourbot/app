# MVP Tickets (Initial 10)

1. App foundation + auth + env setup
2. Postgres schema + migrations + seed
3. Deck CRUD API + dashboard listing
4. Editor shell (left rail / canvas / properties)
5. Deck item CRUD + drag reorder
6. AI deck draft generation endpoint + UX
7. AI rewrite actions (shorten/punchier/formal/casual)
8. Theme/brand system + apply flow
9. Export pipeline (JSON + print/PDF)
10. Shareable read-only deck links + viewer
